<!DOCTYPE html>
<html lang="es">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Detalles del Producto</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.1/css/all.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #37393b;
      color: #ffffff;
      margin: 0;
    }

    .product-details-container {
      text-align: center;
      min-height: 90vh;
      display: grid;
      place-content: center;
    }

    .product-details {
      background-color: #212529;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
      border-radius: 10px;
      padding: 20px;
      width: 300px;
    }

    .product-title {
      color: #ffc107;
      font-size: 1.5rem;
      margin-bottom: 10px;
    }

    .product-description {
      color: #adb5bd;
      margin-bottom: 15px;
    }

    .product-price {
      color: #28a745;
      margin-bottom: 10px;
    }

    .product-stock {
      color: #dc3545;
      margin-bottom: 10px;
    }

    .icon {
      font-size: 24px;
      margin-right: 8px;
    }
  </style>
</head>

<body>
  <div class="product-details-container">
    <div class="product-details">
      <h2 class="product-title">{{product.title}}</h2>
      <p class="product-description"><i class="icon fas fa-align-left"></i>{{product.description}}</p>
      <p class="product-price"><i class="icon fas fa-tag"></i>${{product.price}}</p>
      <p class="product-stock"><i class="icon fas fa-warehouse"></i>Stock: {{product.stock}}</p>
    </div>
  </div>

  <script src="https://kit.fontawesome.com/a076d05399.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>